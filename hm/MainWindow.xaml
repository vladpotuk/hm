<Window x:Class="SearchApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Search App" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10">
            <ComboBox x:Name="cmbSearchEngines" Width="200" Margin="0,0,10,0">
                <ComboBoxItem Content="Bing" Tag="https://www.bing.com/search?q="/>
                <ComboBoxItem Content="Google" Tag="https://www.google.com/search?q="/>
        
            </ComboBox>
            <TextBox x:Name="txtSearchQuery" Width="400" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <Button Content="Search" Click="SearchButton_Click" VerticalAlignment="Center"/>
        </StackPanel>

        <ListBox Grid.Row="1" x:Name="lstSearchResults" Margin="10" VerticalAlignment="Stretch">
           
        </ListBox>
    </Grid>
</Window>
